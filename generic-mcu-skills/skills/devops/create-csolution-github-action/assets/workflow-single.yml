# generated with AI
# Adapted by the create-csolution-github-action skill.

name: __WORKFLOW_NAME__

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [__DEFAULT_BRANCH__]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: __JOB_NAME__
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Activate vcpkg environment
        uses: ARM-software/cmsis-actions/vcpkg@v1
        with:
          config: __VCPKG_CONFIG__
          cache: __CACHE_KEY__

      # The default Arm license setup is for evaluation only.
      - name: Activate Arm tool license
        uses: ARM-software/cmsis-actions/armlm@v1

      - name: Build __TARGET_TYPE__
        working-directory: "__SOLUTION_DIR__"
        run: cbuild "__SOLUTION_FILE__" --active "__TARGET_TYPE__" --packs

      - name: Run __TARGET_TYPE__ on FVP simulation
        working-directory: "__SOLUTION_DIR__"
        run: |
          set -o pipefail
          "__FVP__" \
            -f "__FVP_CONFIG__" \
            -a out/*/"__TARGET_TYPE__"/*/*.hex \
            --simlimit 120 2>&1 | tee fvp.log

      - name: Check FVP output
        working-directory: "__SOLUTION_DIR__"
        run: |
          # Replace this stub with confirmed application output checks.
          test -s fvp.log
__FVP_OUTPUT_CHECKS__

      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: __ARTIFACT_NAME__
          path: |
            __SOLUTION_DIR__/out
            __SOLUTION_DIR__/fvp.log
          if-no-files-found: warn
